name: siete_media
description: Juego de la baraja española.
version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^0.15.1
  url_launcher: ^5.4.5
  universal_html: ^1.2.1
  flutter_launcher_icons: ^0.7.5
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter_icons:
  image_path: "assets/images/icono.png"
  android: true
  ios: true


flutter:
  uses-material-design: true

  assets:
  - assets/images/
  - assets/audio/
